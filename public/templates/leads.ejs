
  <div class="container">
	  <ul class="nav nav-pills">
	       <li ng-class="{ active:isSet(1) }"><a href ng-click="setTab(1)">Completed form</a></li>
	       <li ng-class="{ active:isSet(2) }"><a href ng-click="setTab(2)">Change values</a></li>
	  </ul>
	<div class="" ng-show="isSet(1)">
			<div ng-repeat="le in leads">
				<pre>First Name: {{ le.firstName }}</pre>
			</div>
	 </div>

	<div class="" ng-show="isSet(2)">
	
	 </div>
 </div>